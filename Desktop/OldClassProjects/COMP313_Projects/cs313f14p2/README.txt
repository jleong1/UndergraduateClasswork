In TestIterator
// TODO also try with a LinkedList - does it make any difference?
        //LinkedList is faster performance wise than ArrayList

TODO what happens if you use list.remove(77)?
                // if you use list.remove(77), it will only remove the first occurrence of 77
                // in the list, not as you are iterating the list, so it would not remove all 77s
In TestPerformance
// TODO run test and record running times for SIZE = 10, 100, 1000, 10000
    /* running time for size 10 is .257s
       running time for size 100 is .376s
       running time for size 1000 is 2s
       running time for size 10000 is 20s
     */
	// which of the two lists performs better as the size increases?
    // As size increases, linked lists perform better

In TestList
// TODO also try with a LinkedList - does it make any difference?
        //.There are differences in performances, LinkedList has a faster performance time than ArrayList
	}